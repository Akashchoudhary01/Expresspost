<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <title>Edit Post</title>
</head>
<body class="bg-zinc-800 text-zinc-300 min-h-screen flex items-center justify-center">
    <div class="w-full max-w-md bg-zinc-900 p-8 rounded-lg shadow-[5px_5px_12px_white]">
        <!-- Back Link -->
        <div class="w-full flex justify-end mb-4">
            <a href="/profile" class="text-blue-400 hover:underline">⬅️ Back</a>
        </div>

        <!-- Edit Post Heading -->
        <h2 class="text-2xl font-bold text-white mb-4 text-center">Edit Your Post</h2>

        <!-- Edit Post Form -->
        <form method="post" action="/update/<%= post._id %>" class="flex flex-col gap-4">
            <textarea 
                name="content" 
                placeholder="What's on your mind?" 
                class="w-full h-32 p-3 outline-none text-white bg-transparent border-2 border-zinc-700 rounded-md resize-none"
            ><%= post.content %></textarea>

            <!-- Update Button -->
            <input 
                class="w-full px-3 py-2 bg-yellow-500 text-black font-bold rounded-md cursor-pointer hover:bg-yellow-600 transition duration-300" 
                type="submit" 
                value="Update Post"
            >
        </form>

        <!-- Delete Button -->
        <form method="post" action="/delete/<%= post._id %>" class="mt-4">
            <input 
                class="w-full px-3 py-2 bg-red-500 text-white font-bold rounded-md cursor-pointer hover:bg-red-600 transition duration-300" 
                type="submit" 
                value="Delete Post"
                onclick="return confirm('Are you sure you want to delete this post?')"
            >
        </form>
    </div>
</body>
</html>
