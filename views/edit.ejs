<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <title>Edit Post</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-800 text-zinc-300 p-10">
        <div class="w-full flex justify-end">
            <a href="/profile">⬅️back</a>
        </div>

        <h5 class="p-1">You can edit your Post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea name="content" placeholder="What's on your mind" class="justify-center text-center w-1/3 p-2 outline-none text-white resize-none bg-transparent border-2 border-zinc-900 rounded-md">
                <%= post.content %>
            </textarea>
            <input class="px-3 py-2 mt-2 text-black bg-yellow-500 block m-.5 rounded-mb w-40" type="submit" value="Update Post">
        </form>
    </div>
</body>
</html>
